# 代码清理计划

## 🗑️ 需要删除的文件

### 1. 旧版本/备份文件
- `server.old.js` - 旧版服务器文件（已被 src/app.js 替代）
- `anti_trafficking_system (2).tsx` - 重复文件（已在 anti-trafficking-app/src/ 中）

### 2. 独立测试脚本
- `count-appearances.js` - 独立的出现次数统计脚本（功能已集成到主应用）
- `extract-positions.js` - 独立的位置提取脚本（功能已集成到主应用）
- `sam3-video.js` - 独立的 SAM3 视频测试脚本（仅用于测试）

### 3. 输出文件
- `output.mp4` - 测试输出视频（2.3 MB，已被 .gitignore 忽略）

### 4. 临时目录
- `.trae/` - 临时配置目录

## ✅ 保留的文件

### 核心代码
- `index.js` - 主入口文件
- `src/` - 主应用源代码
- `public/` - 前端静态文件
- `anti-trafficking-app/` - React 应用

### 配置文件
- `package.json`
- `.env.example`
- `.gitignore`

## 📊 清理前后对比

**清理前：**
- 根目录文件：15 个
- 冗余代码：~630 行

**清理后：**
- 根目录文件：10 个
- 更清晰的项目结构

## 🔧 清理操作

删除以下文件：
1. server.old.js
2. anti_trafficking_system (2).tsx
3. count-appearances.js
4. extract-positions.js
5. sam3-video.js
6. output.mp4 (已被 gitignore)

保留但不推送到 GitHub:
- .trae/ (已在 .gitignore 中)
- outputs/ (已在 .gitignore 中)
